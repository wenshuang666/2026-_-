·（考察知识点：像素的概念）

数字图像是网格化的像素，是像素在空间维度上的二维排列，可用一个或多个矩阵进行描述。

每个像素具有空间和颜色两方面属性。

空间属性：该像素在图像中的横坐标。像素的位置(x, y)，指的是该像素在水平方向处于第x个位置，在垂直方向处于第y个位置。

颜色属性：像素的颜色f(x, y)表示(x, y)位置的像素颜色。注意：一个像素的颜色是唯一的，但颜色表达需要根据图像类型的不同而采用不同的表达方式。

·（考察知识点：图像的量化）

举例理解：如果图像的量化位数是 1，则只能表示 21=2 种颜色数量，可用来表示二值图像的颜色；如果图像的量化位数是 8，则可表示 28=256 种颜色数量，可用来表示灰度图像的 256 个等级，也可以用来表示彩色图像的 256 种颜色；如果使用 3 字节（共 24 位）的二进制数进行图像量化，则有 224 =16777216 种颜色，这个颜色数量已经超越了人眼对颜色的分辨能力，通常将这种量化位数的彩色图像称为真彩色图像。

·（考察知识点：收集工业需求）

收集工业需求包括：功能需求和性能需求

功能需求概念：功能需求指的是系统需要实现的功能

功能需求举例：工业字符图像中的字符的识别；工业产品中的缺陷区域的识别和缺陷数量的统计；工业生产过程中的人员操作规范性监控；工业生产过程中的设备状态监控等

性能需求概念：性能需求指的是工业数字图像处理或机器视觉系统应达到的处理速度、识别准确率等指标。

性能需求距离：工业字符图像中的字符识别准确率应达到 99.99%；工业产品中的缺陷数量检测准确率达到 98.00%；生产设备人员进入危险区域的报警准确率达到 100%；误报率低于 5%，报警的响应时间在 500ms 以下，这些都属于性能指标。

·（考察知识点：频域滤波）

图像滤波包括空间域滤波和频域滤波。

空间滤波：均值滤波、高斯加权均值、中值滤波、双边滤波都是空间域滤波。从空域的角度看，就是二维的像素阵列。空域滤波是基于像素之间的几何位置关系进行的。

频域滤波：从频域的角度看，数字图像中含有不同的频率成分，频域滤波就是滤除不需要的频率成分。

频域滤波原理：对于频域滤波，首先将图像转换到频域，然后对不同的频率成分进行滤除，以保留有效频率成分

·（考察知识点：损失函数）

机器学习解决问题：包括分类问题和回归问题。

分类问题：分类问题的输出为有限个离散值。

分类问题示例：预测明天是晴天、雨天还是多云；预测某个字符是0-9中具体哪一个，这些都是典型的分类问题。

回归问题：回归问题的输出为无限个连续值

回归问题示例：预测明天天气的气温是多少度；预测某个字符出现在图像中的位置，这些都是典型的回归问题

·（考察知识点：神经网络的拓扑结构）篇幅很多，肯定会考。

神经网络由多个神经元按照一定拓扑结构连接而成。大多数神经网络的拓扑结构都前馈神经网络（重点研究重点）。

前馈神经网络：由多层神经元组成，包括输入层、隐藏层、输出层。

![](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/3b1d2252-6ca0-4756-92d8-033d1faf42b2/a6f182efac7fbfe7d07d00598c91365db38d9a43bec81a03498010308dee1930.jpg)

连接方式如图所示：

输入层、隐藏层、输出层（各层顺序连接），输入层经过隐藏层到输出，进行单向流动

输入层：图中x1和x2

网络的输出：图中o1和o2

隐藏层:h1，h2和h3组成神经网络的一个隐藏层

特点：同一层内的神经元之间没有连接

信息处理:前馈神经网络的信息处理能力主要由隐藏层层数、拓扑结构、激活函数等决定

模型参数:神经元之间的连接权重就是模型的参数

模型训练过程实际上是不断地调整神经元之间的连接权重,降低损失函数的值；训练好的模型就是一个调整优化后的神经元连接参数及网络拓扑结构。

典型代表：这种前馈神经网络的典型代表是反向传播（Back Propagation，BP）

·（考察知识点：LeNet5）

网络结构：

<table><thead><tr><th><p><strong>层编号</strong></p></th><th><p><strong>类型</strong></p></th><th><p><strong>详细结构</strong></p></th><th><p><strong>输出大小</strong></p></th></tr></thead><tbody><tr><td><p>输入层</p></td><td><p>输入图像</p></td><td><p>32×32×1</p></td><td><p>32×32×1</p></td></tr><tr><td><p>C1</p></td><td><p>卷积层</p></td><td><p>6个5×5卷积核，stride=1，无padding</p></td><td><p>28×28×6</p></td></tr><tr><td><p>S2</p></td><td><p>池化层（亚采样）</p></td><td><p>平均池化2×2，stride=2</p></td><td><p>14×14×6</p></td></tr><tr><td><p>C3</p></td><td><p>卷积层</p></td><td><p>16个卷积核，部分连接（不是全连接）</p></td><td><p>10×10×16</p></td></tr></tbody></table>

·（考察知识点：Inception模块）

一个标准的Inception模块包括四条并行分支：

<table><tr><td><p>分支</p></td><td><p>操作</p></td></tr><tr><td><p>分支1</p></td><td><p>1×1 卷积</p></td></tr><tr><td><p>分支2</p></td><td><p>1×1 卷积 → 3×3卷积</p></td></tr><tr><td><p>分支3</p></td><td><p>1×1 卷积 → 5×5 卷积</p></td></tr><tr><td><p>分支4</p></td><td><p>3×3 最大池化 →1×1 卷积</p></td></tr></table>

所有分支的输出在通道维度拼接（concat）

注意：每个卷积之前都可能加一个 1×1 卷积，目的是降维减少参数量和计算量。