简答题文字量会相对较大。我尽量弄全。所以看着会比较多

·视觉注意力模块。常见视觉注意力模块分为三种

通道注意力：关注 "哪个通道" 比较重要。

强调不同特征类别（如边缘、纹理、颜色等）的重要性差异。

每一个通道可以看成一种特征描述。

有些特征对于当前任务更关键，应该赋予更高的权重。

通道注意力模块通过学习出一组通道权重，动态调整每个通道的贡献

空间注意力：关注 "输入特征图的哪个位置" 更重要。

强化关键区域（如目标区域），弱化背景区域。

在固定通道数的情况下，对输入特征图的每个空间位置学习一个空间权重图。

空间注意力在平面（H×W）上进行加权。

混合注意力：同时关注 "通道重要性" 和 "空间位置重要性"。

二者结合可以带来更强的特征增强效果。

先计算通道注意力，再计算空间注意力（或反过来）。

按照顺序串联使用，整体提升网络关注力。

<table><thead><tr><th><p><strong>类型</strong></p></th><th><p><strong>关注的是什么</strong></p></th><th><p><strong>典型方法</strong></p></th></tr></thead><tbody><tr><td><p>通道注意力（Channel Attention）</p></td><td><p>哪些通道重要</p></td><td><p>SE模块、ECA模块</p></td></tr><tr><td><p>空间注意力（Spatial Attention）</p></td><td><p>哪些空间位置重要？</p></td><td><p>CBAM模块</p></td></tr><tr><td><p>混合注意力（空间+通道）</p></td><td><p>综合考虑</p></td><td><p>CBAM、BAM、Transformer-based Attention</p></td></tr></tbody></table>

·模型过拟合与欠拟合

训练样本和验证样本不是相同的。深度学习训练出的模型在训练数据上和验证数据上的表现可能差异较大。因此会出现过拟合与欠拟合。

什么是过拟合：若模型在训练数据上获得了较小的误差损失，而在验证数据上的误差损失较大，则称为模型过拟合

过拟合出现的原因：可能是特征维度太多、模型过于复杂、参数过多、训练数据太少等

什么是欠拟合：若模型在训练数据上误差一直较大，无法找到合适的模型表达来描述数据集，则称为模型欠拟合

欠拟合出现的原因：欠拟合的可能原因是训练不充分、训练数据太少、训练数据中噪声过多、超参数设置不合理等

·Dropout

什么是Dropout：Dropout 是一种正则化（regularization）技术，在神经网络训练过程中，随机"丢弃"一部分神经元（让它们临时失效）

计算方法（我猜应该不会考，但还是写一下）：如果 p 是保留一个神经元的概率。训练时，神经元以概率 p 保留，概率 1−p 被置零。测试时，使用全部神经元，但输出乘以 p（或训练时直接scale）

如果 zi=1（神经元被保留），原输出照常保留。

如果 zi=0（神经元被drop掉了），也就是这个神经元暂时不起作用

$$
\tilde{h}_{i}=h_{i}\times z_{i}
$$

hi​ 是原神经元输出

zi∼Bernoulli(p)，是一个0或1的随机变量