·（考察知识点：图像的分类）

数字图像概念：数字图像指的是具有一定分辨率和一定的颜色数量（或量化深度）的图像，组成图像的每个像素均具有一个唯一且特定的颜色值，相邻像素的颜色值变化是离散的。数字计算机中的图像就是数字图像。

·（考察知识点：数字图像的分类）

灰度图像：灰度图像指的是每个像素的亮度由不同的灰度等级描述的图像。灰度图像中不包含色彩信息，可以简单地认为灰度图像只有一个亮度通道。

注意：常见的灰度图像等级为256，即256色灰度图像。可以简单地将灰度图像理解为从彩色图像中去除色彩信息而仅保留亮度信息。可以认为颜色是由亮度和色彩两部分构成的。

·（考察知识点：什么是图像的数字化）

图像数字化包括采样、量化和编码三个阶段

图像的数字化过程实际上是一个对模拟图像进行离散化的过程。

采样作用：完成空间维度的离散化

量化作用：完成颜色维度的离散化

数字图像中，颜色的表示是一个较为复杂的问题。涉及颜色空间或颜色模式的概念。

·很可能会考，但是我不知道这算什么知识点：像素的空间关系，包括邻域、邻接、通路。

·（考察知识点：数字图像中像素间的空间关系）

![](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/91c2d79e-cd9c-433f-969e-669f1d7fdab1/b862624351f6e516ba4978d33a13aa4006b3e61be713bfea974cd7c45b7bc40e.jpg)

像素间的邻接关系：

对于数字图像来说，这种邻接需要综合考虑两个要素：物理位置（物理位置上的邻接性，即邻域）、灰度值（灰度值上的邻接性即值域）

·（考察知识点：直方图均匀化）

定义：直方图均匀化（Histogram Equalization）指的是将原始图像的灰度直方图从比较集中的某个灰度区间变成在全部灰度范围内的较为均匀地分布，即实现对图像灰度分布的拉伸。

目的：直方图均匀化就是重新分配像素灰度值，使得各个范围内像素数量大致相同，达到“均匀化”的目的，从而增强图像整体对比度。

·（考察知识点：Inception模块）

计算过程如下

![](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/91c2d79e-cd9c-433f-969e-669f1d7fdab1/6d179bf6da068a8757b7dccd601494660436bf77a29f3612b52433db64a6654a.jpg)

输入特征图。

设输入尺寸为： $H_{in}=28。W_{in}=28。C_{in}=256$

分支1：1×1卷积（输出64通道）

（1）输出尺寸公式： $H_{out}=\left⌊\frac{H_{in}+2P-K}{S}\right⌋+1$

$$
W_{out}=\left⌊\frac{W_{in}+2P-K}{S}\right⌋+1
$$

K=1(卷积核大小)，S=1（步幅Stride），p=0(Padding)

输出通道数是64，所以最终输出尺寸为： $H_{out}=\frac{28+2\times 0-1}{1}+1=28,$

$$
W_{out}=\frac{28+2\times 0-1}{1}+1=28
$$

即28x28x64

2. 参数量公式： $Parameters=K\times K\times C_{in}\times C_{out}$

Parameters=1\*1\*256\*64=16384

分支2：1×1卷积 → 3×3卷积（输出128通道）

(1)K=3，设P=1（padding 1像素，使尺寸不变），stride=1

$$
\Pi _{cut}=\left⌊\frac{28+2\times 1-3}{1}\right⌋+1-28
$$

$$
W_{out}=\left⌊\frac{28+2\times 1-3}{1}\right⌋+1=28
$$

输出尺寸：28x28x128

(2)参数量公式: $Parameters=K\times K\times C_{in}\times C_{out}$

Parameters=3\*3\*96\*128=110592

·（考察知识点：数字图像中的噪声）

按照噪声的统计特性，可以将数字图像中的噪声分为平稳噪声和非平稳噪声。

·（考察知识点：中值滤波）

![](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/91c2d79e-cd9c-433f-969e-669f1d7fdab1/297b77e45c36180184f9228ddf3a1ffa3c931edf876f1fbc1c17a9bf28f69b05.jpg)

具体操作时，从滤波窗口中选择奇数个像素值进行排序，选择中间值作为目标像素值。

·（考察知识点：Canny算子）

原理如下：

首先，使用高斯平滑滤波器平滑图像以除去噪声；

其次，采用一阶微分的有限差分计算梯度幅值和方向，使用非极大值抑制进行“瘦边”；

最后，采用双阈值调整检测出的边缘。

·（考察知识点：Roberts算子）

Roberts算子的模板（分别是X方向和Y方向）：

<table><tr><td><p>-1</p></td><td><p>0</p></td></tr><tr><td><p>0</p></td><td><p>1</p></td></tr></table>

<table><tr><td><p>0</p></td><td><p>-1</p></td></tr><tr><td><p>1</p></td><td><p>0</p></td></tr></table>

例题：假设我们有一个小的灰度图像，大小为5x5像素，我们将使用Roberts算子来检测这个图像中的边缘。

求最终的梯度值

·（考察知识点：视觉注意力模块）

经典注意力模块：SE（Squeeze-and-Excitation）模块

SE的结构与工作流程：Squeeze（压缩） → Excitation（激励） → Scale（重标定）

Squeeze（压缩）：将特征图的空间维度 H×W 压缩，只保留每个通道的全局信息。

即，将 H×W汇聚为一个数字，表示该通道的总体重要性

Excitation（激励）：建模通道之间的相互依赖关系，生成一组通道权重。

Scale（重标定）：用学习到的通道权重，重新调整输入特征图